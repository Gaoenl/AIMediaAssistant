# AIMA 开发方案(MVP)

> 文档版本:v1.0.0 | 日期:2026-08-27 | 关联文档:[AIMA-技术规划方案.md](AIMA-技术规划方案.md)

---

## 1. 目标与范围

8 周交付可运行的 MVP:覆盖 **感知 → 创作 → 编辑 → 审核 → 分发(公众号)→ 统计** 全链路。

**本次不做**:第三档模拟发布、n8n、AI 配图、多租户、Chrome 插件、机器人接入、短视频生产。

## 2. 技术栈

| 端 | 技术 | 说明 |
| --- | --- | --- |
| Java 后端 | Java 21 / Spring Boot 3.3 / Spring Security + JWT | 模块化单体,包结构即模块边界 |
| ORM | MyBatis-Plus 3.5.x + Flyway | 数据库迁移统一管理 |
| 缓存/限流 | Redis 7 + Redisson | 缓存、限流计数、分布式锁 |
| 消息队列 | RocketMQ 5.x(`rocketmq-spring-boot-starter`) | topic: `aima-generation-task` |
| Python 引擎 | Python 3.11 / FastAPI / uvicorn / pydantic v2 | 独立 AI 服务 |
| Agent 编排 | LangGraph | 感知/创作/编辑/质检 |
| Python MQ | `rocketmq-client-python` | 消费生成任务 |
| Python DB | SQLAlchemy 2.x(只读 MySQL)+ psycopg(pgvector) | 业务表只读,向量库写入 |
| Embedding | sentence-transformers + `BAAI/bge-m3` | 本地部署,向量 1024 维 |
| 前端 | Vue 3 + Vite + Element Plus + Pinia + Axios | 控制台 |
| 数据 | MySQL 8.0 / PostgreSQL 16 + pgvector / Redis 7 | 见部署章节 |
| 部署 | Docker Compose + Nginx | 单机起步 |

## 3. 仓库结构

```
AIMediaAssistant/
├─ docs/                     # 方案与调研文档
├─ aima-java/                # Java 模块化单体(单 Maven 工程,按功能分包)
│   ├─ src/main/java/com/aima/
│   │   ├─ auth/             # 用户、登录、JWT、RBAC
│   │   ├─ project/          # 项目、选题(topic)
│   │   ├─ content/          # 文章、generation_task、质量评估门禁
│   │   ├─ publish/          # platform_account、publish_task、平台适配、webhook
│   │   ├─ stats/            # 统计回收
│   │   ├─ scheduler/        # @Scheduled + Redisson 锁
│   │   └─ common/           # 统一响应、异常、工具、内部客户端
│   └─ src/main/resources/db/migration/   # Flyway 迁移
├─ aima-python/              # Python AI 引擎
│   ├─ app/
│   │   ├─ api/              # /internal/* 路由(collect、callback 接收)
│   │   ├─ agents/           # perception / creation / editing / quality
│   │   ├─ models/           # 模型适配层(DeepSeek/Qwen/混元)
│   │   ├─ services/         # 采集、向量、RAG
│   │   └─ consumers/        # RocketMQ 消费者
│   ├─ tests/
│   └─ requirements.txt
├─ aima-web/                 # Vue 3 控制台
├─ deploy/
│   ├─ docker-compose.yml    # mysql/pgvector/redis/rocketmq/nginx
│   ├─ nginx/
│   └─ env.example
└─ README.md
```

## 4. 数据库落地清单

**MySQL(Flyway 管理)**

- `user`、`project`、`topic`、`article`、`generation_task`、`platform_account`、`publish_task`、`material`、`style_profile`、`audit_log`
- 字段以技术方案第 3 节为准;`publish_task` 增加 `callback_at`、`callback_payload_json` 字段
- `generation_task`、`publish_task` 建状态索引,便于巡检与统计

**PostgreSQL + pgvector**

- `embedding(id, entity_type, entity_id, chunk_text, embedding vector(1024), created_at)`
- HNSW 索引(`vector_cosine_ops`);预置全量重建脚本

**Redis**

- 登录 token / 刷新令牌、限流计数、生成结果缓存(MD5(style_prompt+topic))

## 5. 模块任务拆解(M1–M4)

### M1(第 1–2 周):基础设施与链路打通

- 初始化仓库、Docker Compose(mysql/pgvector/redis/rocketmq)、Flyway 首版迁移
- Java 骨架:统一响应/异常、JWT 登录、RBAC(admin/user)
- RocketMQ:Java 生产者 + Python 消费者,消息只带 `taskId`,空跑后回调 Java
- Python 骨架:FastAPI、`/internal` 路由、健康检查、模型适配层接口(先 Mock)
- 内部契约:OpenAPI yaml 定义 Java↔Python 的 `collect` / `callback` 接口
- 前端骨架:登录、布局、任务列表(轮询)
- **验收**:前端提交生成 → MQ → Python 消费 → 回调 → 状态更新,端到端跑通

### M2(第 3–4 周):采集与创作

- 感知 Agent:知乎热榜、B站热门、36氪/虎嗅 RSS、节日日历采集器;归一化 + 去重入库 `topic`
- 创作 Agent:大纲 → 正文(默认 DeepSeek),风格模板 2 个(小红书口语化/知乎深度)
- 编辑 Agent:AI 高频词过滤、自动分段加 h3、配图建议(手动选图)
- 质量评估:规则(敏感词/AI 词/字数)+ LLM 打分,默认 75 分门禁,不合格打回重写(上限 2 次)
- 前端:选题池、文章编辑器、生成进度
- **验收**:能批量生成通过质检的文章草稿

### M3(第 5–6 周):审核与分发

- 审核状态机 `REVIEWING → APPROVED/REJECTED` + 审计日志
- 公众号接入:素材上传、草稿箱、`freepublish/submit`、`PUBLISHJOBFINISH` webhook(验签)、`WAIT_CALLBACK` 状态
- 发布调度:`@Scheduled` + Redisson 锁;每账号每日上限与最小间隔
- 半自动导出:`GET /api/v1/articles/{id}/export`(第二档)
- 前端:审核页、发布任务页、平台账号配置
- **验收**:人工审核后成功发布一篇公众号文章,状态机完整闭环

### M4(第 7–8 周):统计、质量与上线

- 统计回收:公众号阅读/点赞数据拉取 + 总览页
- 成本监控:token 用量按项目/模型统计,月度预算熔断
- 可观测性:JSON 日志 + `taskId` 关联;告警(任务失败、MQ 积压、成本超限)
- 部署:Nginx SSL、docker-compose 生产化、每日备份、CI/CD
- **验收**:全链路稳定运行,单篇成本可控,发布成功率达标

## 6. 关键实现约定

- **数据写入口唯一**:Java 写 MySQL 业务表;Python 只读任务参数、回调结果;Python 写 pgvector
- **异步链路**:Java → RocketMQ(`taskId`)→ Python → HTTP 回调 → Java 落库;前端轮询任务状态
- **幂等**:消费者按任务状态幂等;回调按状态机校验,重复回调直接忽略
- **状态机**:
  - 文章:`DRAFT → REVIEWING → APPROVED → PUBLISHED`,`REJECTED → DRAFT`
  - 生成任务:`PENDING → RUNNING → SUCCESS/FAILED`(业务重试上限 2 次)
  - 发布任务:`PENDING → PUBLISHING → WAIT_CALLBACK → SUCCESS/FAILED`
- **凭据**:平台 token AES-256-GCM 加密存储,密钥走环境变量/KMS,日志脱敏
- **配置**:全部环境变量注入(`deploy/env.example`),本地与测试环境用 mock

## 7. 模型接入(适配层)

- 统一接口:`chat(messages, model, temperature, max_tokens)`,返回内容 + token 用量
- 默认路由:创作用 DeepSeek(OpenAI 兼容),质检用 Qwen-Turbo(DashScope),混元备用
- 降级链:主模型超时/限流 → 备用模型;连续失败熔断 + 告警
- Embedding:本地 bge-m3;预留 API embedding 切换配置

## 8. 测试策略

| 层级 | 方式 |
| --- | --- |
| Java 单测 | JUnit5 + MockMvc:状态机、鉴权、限流、回调验签 |
| Python 单测 | pytest:采集器归一化、质检规则、模型适配(mock) |
| 契约测试 | OpenAPI 文件为唯一契约,Java/Python 各自生成客户端或校验 |
| E2E | docker-compose 全链路冒烟:提交 → 生成 → 审核 → 发布(mock 平台) |

## 9. 团队分工(2–3 人参考)

- **Java 后端**:鉴权、内容/发布/状态机、调度、公众号接入、webhook
- **Python 后端**:采集、创作/编辑/质检 Agent、模型适配、向量/RAG
- **前端**:登录、选题池、创作、审核、发布、统计页面
- 每周一次联调;契约先行,接口定义先于实现

## 10. 前置依赖与风险

| 依赖/风险 | 说明 | 应对 |
| --- | --- | --- |
| 企业主体资质 | 公众号需认证、抖音/B站需企业资质 | M3 前确认主体;无资质则第一档降级半自动 |
| 模型 API Key 与预算 | 生成是 token 消耗大户 | M1 申请 Key,配置预算熔断 |
| 微信 access_token | 多实例并发刷新会互相失效 | 集中管理 + 刷新锁(Redisson) |
| MQ/Python 链路稳定 | 消费失败影响生成 | 消费重试 + DLQ + 告警巡检 |
| 热点源变动 | 接口改版导致采集中断 | 多源冗余 + 健康检查 + 降级 |

## 11. 里程碑总表

| 阶段 | 时间 | 核心交付 | 验收标准 |
| --- | --- | --- | --- |
| M1 | 1–2 周 | 双服务骨架 + MQ 链路 + 鉴权 + 前端骨架 | 提交生成→MQ→Python→回调端到端跑通 |
| M2 | 3–4 周 | 采集 + 选题池 + 创作/编辑/质检 | 批量产出通过质检的草稿 |
| M3 | 5–6 周 | 审核流 + 公众号发布 + 半自动导出 | 人工审核后成功发布一篇 |
| M4 | 7–8 周 | 统计 + 成本监控 + 部署上线 | 全链路稳定,成本可控 |

